'use strict';

/**
 * tenants-devices-groups-device controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tenants-devices-groups-device.tenants-devices-groups-device');
