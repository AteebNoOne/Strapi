'use strict';

/**
 * tenants-ds-gs-device controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tenants-ds-gs-device.tenants-ds-gs-device');
