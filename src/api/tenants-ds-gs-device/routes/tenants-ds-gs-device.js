'use strict';

/**
 * tenants-ds-gs-device router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::tenants-ds-gs-device.tenants-ds-gs-device');
